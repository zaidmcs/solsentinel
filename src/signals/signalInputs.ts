export interface HackathonInput {
  currentDay: number;
  daysRemaining: number;
  hasActivePoll: boolean;
  announcementPresent: boolean;
}

export interface ForumInput {
  highEngagementThreads: number;
  securityMentions: number;
  totalThreads: number;
}

export interface TimeInput {
  hoursRemaining: number;
}
